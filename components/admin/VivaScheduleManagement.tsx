'use client';

import { useMemo, useState } from 'react';
import type { FormEvent } from 'react';
import { CalendarCheck2, CalendarDays, Loader2, XCircle } from 'lucide-react';

import type { VivaPanelDto } from '../../lib/vivaPanelAdmin';
import type { VivaRoundDto, VivaTeamOption } from '../../lib/vivaRoundAdmin';
import type { VivaAutomaticSchedulePreview, VivaScheduleDto } from '../../lib/vivaScheduling';
import { Badge, Button, DashboardPanel, SectionHeader, StyledInput, TextArea } from '../ui';

type AvailabilityDraft = { startsAt: string; endsAt: string; rooms: string };
const isRecord = (value: unknown): value is Record<string, unknown> => Boolean(value) && typeof value === 'object' && !Array.isArray(value);
const readError = (value: unknown, fallback: string) => isRecord(value) && typeof value.error === 'string' && value.error.trim() ? value.error : fallback;
function readSchedule(value: unknown): VivaScheduleDto | null {
  if (!isRecord(value) || typeof value.id !== 'string' || typeof value.roundId !== 'string' || typeof value.panelId !== 'string' || typeof value.projectId !== 'string' || typeof value.scheduledAt !== 'string' || typeof value.vivaEndsAt !== 'string' || typeof value.locationLabel !== 'string' || typeof value.cancellationReason !== 'string' || typeof value.version !== 'number' || !['scheduled', 'running', 'completed', 'cancelled'].includes(String(value.phase))) return null;
  return value as VivaScheduleDto;
}
function readPreview(value: unknown): VivaAutomaticSchedulePreview | null {
  if (!isRecord(value) || !Array.isArray(value.scheduled) || !Array.isArray(value.unplaced)) return null;
  const scheduled = value.scheduled.filter((entry): entry is VivaAutomaticSchedulePreview['scheduled'][number] => isRecord(entry) && typeof entry.projectId === 'string' && typeof entry.panelId === 'string' && typeof entry.scheduledAt === 'string' && typeof entry.vivaEndsAt === 'string' && typeof entry.locationLabel === 'string');
  const unplaced = value.unplaced.filter((entry): entry is VivaAutomaticSchedulePreview['unplaced'][number] => isRecord(entry) && typeof entry.projectId === 'string' && typeof entry.reason === 'string');
  return scheduled.length === value.scheduled.length && unplaced.length === value.unplaced.length ? { scheduled, unplaced } : null;
}
function localInputValue(date: Date): string { const pad = (value: number) => String(value).padStart(2, '0'); return `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())}T${pad(date.getHours())}:${pad(date.getMinutes())}`; }
function formatLocalTime(value: string): string { const date = new Date(value); return Number.isFinite(date.getTime()) ? new Intl.DateTimeFormat(undefined, { dateStyle: 'medium', timeStyle: 'short' }).format(date) : 'Invalid time'; }
function roomList(value: string): string[] { const rooms = value.split('\n').map((room) => room.trim()).filter(Boolean); return [...new Map(rooms.map((room) => [room.toLocaleLowerCase(), room])).values()]; }

export default function VivaScheduleManagement({ round, teams, panels, schedules, isRoundSaving, onSaved, onConfirmed }: { round: VivaRoundDto; teams: VivaTeamOption[]; panels: VivaPanelDto[]; schedules: VivaScheduleDto[]; isRoundSaving: boolean; onSaved: (schedule: VivaScheduleDto) => void; onConfirmed: (round: VivaRoundDto) => void }) {
  const [availability, setAvailability] = useState<AvailabilityDraft>(() => { const now = new Date(); return { startsAt: localInputValue(now), endsAt: localInputValue(new Date(now.getTime() + 4 * 60 * 60_000)), rooms: '' }; });
  const [draft, setDraft] = useState<VivaAutomaticSchedulePreview | null>(null);
  const [error, setError] = useState(''); const [message, setMessage] = useState(''); const [busy, setBusy] = useState<'preview' | 'apply' | 'confirm' | 'cancel' | null>(null); const [cancellingId, setCancellingId] = useState<string | null>(null); const [cancellationReason, setCancellationReason] = useState('');
  const roundTeams = useMemo(() => teams.filter((team) => round.projectIds.includes(team.id)), [round.projectIds, teams]);
  const teamsById = useMemo(() => new Map(roundTeams.map((team) => [team.id, team])), [roundTeams]);
  const roundSchedules = useMemo(() => schedules.filter((schedule) => schedule.roundId === round.id), [round.id, schedules]);
  const disabled = Boolean(round.confirmedAt) || isRoundSaving || busy !== null;
  const preview = async (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const startsAt = new Date(availability.startsAt); const endsAt = new Date(availability.endsAt); const rooms = roomList(availability.rooms); if (!Number.isFinite(startsAt.getTime()) || !Number.isFinite(endsAt.getTime()) || startsAt >= endsAt || rooms.length === 0 || rooms.length > 32) { setError('Choose a valid local time window and between 1 and 32 rooms.'); return; } setBusy('preview'); setError(''); try { const response = await fetch('/api/admin/viva', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ action: 'preview-automatic-schedule', roundId: round.id, availability: rooms.map((locationLabel) => ({ startsAt: startsAt.toISOString(), endsAt: endsAt.toISOString(), locationLabel })) }) }); const body: unknown = await response.json().catch(() => ({})); if (!response.ok) throw new Error(readError(body, 'Unable to generate the Viva schedule draft.')); const next = isRecord(body) ? readPreview(body.draft) : null; if (!next) throw new Error('Viva automatic schedule response was invalid.'); setDraft(next); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Unable to generate the Viva schedule draft.'); } finally { setBusy(null); } };
  const apply = async () => { if (!draft?.scheduled.length) return; setBusy('apply'); setError(''); try { const response = await fetch('/api/admin/viva', { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ action: 'apply-automatic-schedule', roundId: round.id, schedules: draft.scheduled.map(({ projectId, panelId, scheduledAt, locationLabel }) => ({ projectId, panelId, scheduledAt, locationLabel })) }) }); const body: unknown = await response.json().catch(() => ({})); if (!response.ok) throw new Error(readError(body, 'Unable to save the Viva schedule draft.')); const saved = isRecord(body) && Array.isArray(body.schedules) ? body.schedules.map(readSchedule) : null; if (!saved?.every((schedule): schedule is VivaScheduleDto => Boolean(schedule))) throw new Error('Viva automatic schedule response was invalid.'); saved.forEach(onSaved); setDraft(null); setMessage(`${saved.length} Viva sessions scheduled.`); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Unable to save the Viva schedule draft.'); } finally { setBusy(null); } };
  const confirm = async () => { setBusy('confirm'); setError(''); try { const response = await fetch('/api/admin/viva', { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ action: 'confirm-round', roundId: round.id }) }); const body: unknown = await response.json().catch(() => ({})); if (!response.ok) throw new Error(readError(body, 'Unable to confirm this Viva round.')); if (!isRecord(body) || !isRecord(body.round)) throw new Error('Viva confirmation response was invalid.'); onConfirmed(body.round as VivaRoundDto); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Unable to confirm this Viva round.'); } finally { setBusy(null); } };
  const cancel = async (schedule: VivaScheduleDto) => { if (!cancellationReason.trim()) return; setBusy('cancel'); setError(''); try { const response = await fetch('/api/admin/viva', { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ action: 'cancel-session', sessionId: schedule.id, version: schedule.version, cancellationReason }) }); const body: unknown = await response.json().catch(() => ({})); if (!response.ok) throw new Error(readError(body, 'Unable to cancel the Viva session.')); const cancelled = isRecord(body) ? readSchedule(body.schedule) : null; if (!cancelled) throw new Error('Viva cancellation response was invalid.'); onSaved(cancelled); setCancellingId(null); setCancellationReason(''); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : 'Unable to cancel the Viva session.'); } finally { setBusy(null); } };
  return <div className="space-y-6"><DashboardPanel><SectionHeader title="Automatic Team Scheduling" description={round.confirmedAt ? 'This round is confirmed. Sessions are visible to students and scheduling is locked.' : 'Choose a local time window and rooms for concurrent sessions.'} />{error && <p role="alert" className="mb-4 rounded-xl bg-[var(--color-danger-soft)] px-4 py-3 text-sm font-semibold text-[var(--color-danger)]">{error}</p>}{message && <p role="status" className="mb-4 rounded-xl bg-[var(--color-success-soft)] px-4 py-3 text-sm font-semibold text-[var(--color-success)]">{message}</p>}{!round.confirmedAt && <form onSubmit={preview} className="grid gap-4 lg:grid-cols-3"><div><label className="mb-2 block text-sm font-bold">Available from (your local time)</label><StyledInput type="datetime-local" required value={availability.startsAt} disabled={disabled} onChange={(event) => setAvailability((current) => ({ ...current, startsAt: event.target.value }))} /></div><div><label className="mb-2 block text-sm font-bold">Available until (your local time)</label><StyledInput type="datetime-local" required value={availability.endsAt} disabled={disabled} onChange={(event) => setAvailability((current) => ({ ...current, endsAt: event.target.value }))} /></div><div><label className="mb-2 block text-sm font-bold">Rooms, one per line</label><TextArea required value={availability.rooms} placeholder={'Lab 3\nLab 4'} disabled={disabled} onChange={(event) => setAvailability((current) => ({ ...current, rooms: event.target.value }))} /></div><div className="lg:col-span-3 flex justify-end"><Button type="submit" disabled={disabled || roundTeams.length === 0 || panels.length === 0}>{busy === 'preview' ? <Loader2 className="animate-spin" size={16} /> : <CalendarDays size={16} />}Generate automatic draft</Button></div></form>}{draft && <div className="mt-6 space-y-4"><p className="text-sm text-[var(--color-text-muted)]">{draft.scheduled.length} sessions are ready to save unchanged.</p>{draft.scheduled.map((session) => <p key={session.projectId} className="rounded-xl border border-[var(--color-border)] p-3 text-sm"><strong>{teamsById.get(session.projectId)?.title || 'Unavailable team'}</strong>, {formatLocalTime(session.scheduledAt)}, {session.locationLabel}</p>)}{draft.unplaced.length > 0 && <div className="rounded-xl border border-[var(--color-border)] p-4"><p className="font-bold">Will be held if this round is confirmed</p>{draft.unplaced.map((entry) => <p key={entry.projectId} className="text-sm text-[var(--color-text-muted)]">{teamsById.get(entry.projectId)?.title || 'Unavailable team'}: {entry.reason}</p>)}</div>}<div className="flex justify-end"><Button onClick={() => void apply()} disabled={disabled || draft.scheduled.length === 0}>{busy === 'apply' ? <Loader2 className="animate-spin" size={16} /> : <CalendarDays size={16} />}Save automatic sessions</Button></div></div>}{!round.confirmedAt && <div className="mt-6 flex justify-end"><Button variant="success" onClick={() => void confirm()} disabled={disabled}>{busy === 'confirm' ? <Loader2 className="animate-spin" size={16} /> : <CalendarCheck2 size={16} />}Confirm round</Button></div>}{round.confirmedAt && <div className="mt-5 rounded-xl border border-[var(--color-border)] p-4"><p className="font-bold">Held for a later round</p><p className="mt-1 text-sm text-[var(--color-text-muted)]">{round.heldProjectIds.length ? round.heldProjectIds.map((id) => teamsById.get(id)?.title || id).join(', ') : 'No teams are on hold.'}</p></div>}</DashboardPanel><DashboardPanel><SectionHeader title="Scheduled sessions" description="Only cancellation remains available after confirmation." />{roundSchedules.map((schedule) => <div key={schedule.id} className="mb-3 rounded-xl border border-[var(--color-border)] p-4"><div className="flex flex-wrap items-center justify-between gap-3"><p><strong>{teamsById.get(schedule.projectId)?.title || 'Unavailable team'}</strong><span className="block text-sm text-[var(--color-text-muted)]">{formatLocalTime(schedule.scheduledAt)} to {formatLocalTime(schedule.vivaEndsAt)}, {schedule.locationLabel}</span></p><Badge variant={schedule.phase === 'cancelled' ? 'warning' : 'success'}>{schedule.phase}</Badge>{schedule.phase === 'scheduled' && <Button variant="danger" onClick={() => setCancellingId(schedule.id)} disabled={busy !== null}><XCircle size={16} />Cancel</Button>}</div>{cancellingId === schedule.id && <div className="mt-3"><TextArea value={cancellationReason} maxLength={1000} onChange={(event) => setCancellationReason(event.target.value)} placeholder="Cancellation reason" /><div className="mt-2 flex justify-end gap-2"><Button variant="ghost" onClick={() => setCancellingId(null)}>Keep session</Button><Button variant="danger" onClick={() => void cancel(schedule)} disabled={busy !== null || !cancellationReason.trim()}>Confirm cancellation</Button></div></div>}</div>)}</DashboardPanel></div>;
}
